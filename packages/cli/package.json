{
  "name": "@modula/cli",
  "version": "0.1.0",
  "description": "One-command registration CLI for the Modula protocol.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "modula": "./bin/modula.js"
  },
  "files": ["bin", "dist", "README.md"],
  "scripts": {
    "build":     "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean":     "rm -rf dist"
  },
  "dependencies": {
    "@inquirer/prompts": "^7.2.0",
    "viem":              "^2.48.4"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript":  "^5.6.0",
    "tsx":         "^4.21.0"
  }
}
